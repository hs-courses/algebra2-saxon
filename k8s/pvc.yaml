apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: algebra2-data
  namespace: algebra2
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
